# Normalize line endings: LF in the repository, regardless of contributor OS.
* text=auto eol=lf

# Binaries — never touch
*.png binary
*.jpg binary
*.webp binary
*.gif binary
*.ttf binary
*.otf binary
*.jks binary
*.keystore binary
